<% include ad_header %>
    <div class="row">
        <div class="col-lg-12">
            <!--add new menu item-->
            <div class="col-lg-6">
                <%if (message.length > 0 ){%>
                    <div class="alert alert-danger">
                        <h4>
                            <%= message%>
                        </h4>
                    </div>
                    <%}%>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <!--add new menu item-->
            <div id="addMenu" class="col-lg-6">
                <br />
                <div class="panel panel-default">
                    <div class="panel-heading text-center">
                    </div>
                    <form action="/admin/style/add-new" method="POST">
                        <input type="hidden" name="_token" value="{{csrf_token()}}" />
                        <div class="form-group">
                            <label for="email">Style name:</label>
                            <input type="text" class="form-control" name="stylename" placeHolder="Style name" required />
                        </div>
                        <button type="submit" class="btn btn-default">Add</button>
                        <button type="reset" class="btn btn-default">Reset</button>
                    </form>
                    <br>
                    <div class="clearfix"></div>
                </div>
            </div>
            <!--end add new menu item-->
        </div>
    </div>


    <% include ad_footer%>